screens.login=()=>authScreen('login');screens.register=()=>authScreen('register');
function authScreen(mode){const isLogin=mode==='login';return `<section class="auth-page"><aside class="auth-art"><div class="auth-brand"><svg><use href="#i-logo"/></svg><span>ceres</span></div><div class="auth-orbit"></div><div class="auth-copy"><h1>Cultivate your academic life.</h1><p>One calm workspace for lectures, assignments, notes, revision, planning and collaboration.</p></div><small class="muted">Product prototype · Authentication flow</small></aside><main class="auth-form-wrap"><form class="auth-form" onsubmit="return false"><span class="eyebrow">${isLogin?'Welcome back':'Create your workspace'}</span><h2>${isLogin?'Log in to Ceres':'Create an account'}</h2><p>${isLogin?'Continue where you left off.':'Start organising your degree in one connected place.'}</p>${!isLogin?`<div class="field"><label>Username</label><input class="input" placeholder="morgan"></div>`:''}<div class="field"><label>Email${isLogin?' or username':''}</label><input class="input" placeholder="morgan@example.com"></div><div class="field"><label>Password</label><input type="password" class="input" value="password123"></div>${!isLogin?`<div class="field"><label>Confirm password</label><input type="password" class="input" value="password123"></div>`:''}<button class="button primary" data-route="${isLogin?'dashboard':'onboarding'}">${isLogin?'Log in':'Create account'}</button><div class="auth-alt">${isLogin?'New to Ceres? <button class="text-button" data-route="register">Create an account</button>':'Already have an account? <button class="text-button" data-route="login">Log in</button>'}</div><div class="auth-alt"><button class="text-button" data-route="dashboard">Return to prototype</button></div></form></main></section>`}

screens.settings=()=>`${pageHeader('Account','Settings','Manage your identity, privacy, notifications, accessibility and the visual theme used across every Ceres screen.',`<button class="button">Save changes</button>`)}<div class="settings-shell"><nav class="settings-nav"><button class="settings-link active">${icon('palette')} Appearance</button><button class="settings-link">${icon('user')} Profile</button><button class="settings-link">${icon('settings')} Account</button><button class="settings-link">${icon('lock')} Privacy</button><button class="settings-link">${icon('bell')} Notifications</button><button class="settings-link">${icon('clock')} Time & language</button><button class="settings-link" data-route="onboarding">${icon('arrow')} Onboarding preview</button><button class="settings-link" data-route="login">${icon('lock')} Login preview</button></nav><main class="settings-panel"><section class="settings-section"><h2>Appearance</h2><p>Choose from three dark and three light themes. The selection applies immediately across the complete interface.</p><h3 class="theme-mode-label">Dark</h3><div class="theme-settings-grid">${settingsTheme('forest')}${settingsTheme('moonlit')}${settingsTheme('clay')}</div><h3 class="theme-mode-label">Light</h3><div class="theme-settings-grid">${settingsTheme('daybreak')}${settingsTheme('linen')}${settingsTheme('harvest')}</div></section><section class="settings-section"><h2>Interface</h2><p>Adjust common interface behaviour without changing the underlying theme.</p><div class="setting-row"><div><b>Reduced motion</b><p>Minimise non-essential transitions and movement.</p></div><button class="switch"></button></div><div class="setting-row"><div><b>Compact navigation</b><p>Show icon-only navigation on larger screens.</p></div><button class="switch"></button></div><div class="setting-row"><div><b>High contrast borders</b><p>Increase the separation between cards and surfaces.</p></div><button class="switch on"></button></div></section><section class="settings-section"><h2>Theme behaviour</h2><p>Ceres can follow the operating system or use a fixed preference.</p><div class="setting-row"><div><b>Use selected theme</b><p>Current: <span id="current-theme-setting">Moonlit</span></p></div><select class="select" style="width:180px"><option>Always</option><option>Follow system mode</option><option>Schedule by time</option></select></div></section></main></div>`;
function settingsTheme(name){const t=themes[name];return `<button class="theme-option" data-set-theme="${name}" style="--preview-side:${t.side};--preview-main:${t.main};--preview-accent:${t.accent}"><span class="theme-preview"><aside></aside><main><i></i><i></i></main></span><b>${t.name}</b><small>${t.mode} · ${t.description}</small></button>`}

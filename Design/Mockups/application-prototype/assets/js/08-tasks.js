screens.tasks=()=>`${pageHeader('Planning engine','Tasks','Personal, academic and collaborative work all use one task system with priorities, recurrence, links and assignees.',`<button class="button">${icon('filter')} Filter</button><button class="button primary">${icon('plus')} New task</button>`)}<div class="task-summary-bar"><div class="stat-card"><span>Due today</span><b>4</b></div><div class="stat-card"><span>In progress</span><b>6</b></div><div class="stat-card"><span>Assigned to me</span><b>3</b></div><div class="stat-card"><span>Completed this week</span><b>11</b></div></div><div class="filter-row"><div class="segmented"><button class="segment active">Board</button><button class="segment">List</button></div><button class="filter-chip active">All tasks</button><button class="filter-chip">Personal</button><button class="filter-chip">Assignments</button><button class="filter-chip">Group work</button><button class="filter-chip">Recurring</button></div><section class="task-board">${taskColumn('Not started',[['Complete first essay argument','Environmental Ethics · Due today','urgent'],['Review DNS flashcards','Computer Networks · Due tomorrow',''],['Book study room','Study session · Friday','low']])}${taskColumn('In progress',[['Network topology diagram','Network Design Report · 60%',''],['Portfolio test coverage','Data Structures · 35%','urgent'],['Group presentation slides','Project Team Alpha · Assigned to Morgan','']])}${taskColumn('Completed',[['Build source list','Essay Draft · Completed yesterday','low'],['Subnetting practice questions','Revision · Completed today','low'],['Upload meeting notes','Project Team Alpha · Completed Monday','low']])}</section>`;
function taskColumn(title,tasks){return `<section class="task-column"><header><h3>${title}</h3><span class="badge">${tasks.length}</span></header>${tasks.map(([name,meta,priority])=>`<article class="task-card"><header><span class="priority ${priority}"></span>${icon('more')}</header><h4>${name}</h4><p>${meta}</p><footer><span>${priority==='urgent'?status('Urgent','danger'):priority==='low'?status('Low','success'):status('Medium','warning')}</span><span class="module-avatar-row"><i class="member-avatar">M</i></span></footer></article>`).join('')}<button class="button ghost" style="width:100%;margin-top:8px">${icon('plus')} Add task</button></section>`}
